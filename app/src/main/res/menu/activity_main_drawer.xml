<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:id="@+id/nav_menu_1_group"
        android:checkableBehavior="single">
        <item
            android:id="@+id/nav_home"
            android:icon="@drawable/ic_profile"
            android:title="Home" />
    </group>
    <group android:id="@+id/nav_menu_2_group"
        android:checkableBehavior="single">
        <item
            android:id="@+id/nav_exercises"
            android:icon="@drawable/ic_exercises"
            android:title="Exercises" />
    </group>
    <group android:id="@+id/nav_menu_3_group"
        android:checkableBehavior="single">
        <item
            android:id="@+id/nav_workout"
            android:icon="@drawable/ic_workout"
            android:title="Workout" />
    </group>
    <group android:id="@+id/nav_menu_4_group"
        android:checkableBehavior="single">
        <item
            android:id="@+id/nav_history"
            android:icon="@drawable/ic_history"
            android:title="History" />
    </group>

    <item android:title="">
        <menu>
            <item
            android:id="@+id/nav_profile"
            android:icon="@drawable/ic_edit"
            android:title="Profile" />
            <item
                android:id="@+id/nav_logout"
                android:icon="@drawable/ic_logout"
                android:title="Log out" />
        </menu>
    </item>

</menu>
